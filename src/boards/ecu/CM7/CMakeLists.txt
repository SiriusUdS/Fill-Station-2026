cmake_minimum_required(VERSION 3.22)

#
# This file is generated only once,
# and is not re-generated if converter is called multiple times.
#
# User is free to modify the file as much as necessary
#

# Setup compiler settings
set(CMAKE_C_STANDARD 11)
set(CMAKE_C_STANDARD_REQUIRED ON)
set(CMAKE_C_EXTENSIONS ON)

# C++23 for our own sources (main.cpp etc.). Set before add_executable so the
# target's CXX_STANDARD property initializes correctly.
set(CMAKE_CXX_STANDARD 23)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)

# Define the build type
if(NOT CMAKE_BUILD_TYPE)
    set(CMAKE_BUILD_TYPE "Debug")
endif()

message("Build type: " ${CMAKE_BUILD_TYPE})

# Enable compile command to ease indexing with e.g. clangd
set(CMAKE_EXPORT_COMPILE_COMMANDS TRUE)

# Compiler options
set(STM32_MCU_FLAGS  "-mcpu=cortex-m7 -mfpu=fpv5-d16 -mfloat-abi=hard " )

#Linker options
set(STM32_LINKER_SCRIPT stm32h747xx_flash_CM7.ld)

# Set the project name
set(CMAKE_PROJECT_NAME ecu_CM7)
project(${CMAKE_PROJECT_NAME})

# Create an executable object type
add_executable(${CMAKE_PROJECT_NAME})

# Include mx-generated
include("mx-generated.cmake")


# In order to use CMake for cross-compiling
set(CMAKE_CXX_COMPILER_FORCED "true")

# Add project symbols (macros)
target_compile_definitions(${CMAKE_PROJECT_NAME} PRIVATE
    # Add user defined symbols
)

# Enable CMake support for ASM, C and C++ languages
enable_language(C CXX ASM)

# Pin the language standards on the target explicitly (the target was created
# above by add_executable, so set the properties rather than relying on the
# CMAKE_*_STANDARD variables alone).
set_target_properties(${CMAKE_PROJECT_NAME} PROPERTIES
    C_STANDARD 11
    C_STANDARD_REQUIRED ON
    C_EXTENSIONS ON
    CXX_STANDARD 23
    CXX_STANDARD_REQUIRED ON
    CXX_EXTENSIONS OFF
)

# Add include paths
target_include_directories(${CMAKE_PROJECT_NAME} PRIVATE
    Core/Inc                                                  # generated by cubemx
    ${REPO_ROOT}/src/app                                      # board:: contract (board.hpp)
    ${REPO_ROOT}/src/app/logic/ecu                            # ECU controller + EcuValves
    ${REPO_ROOT}/src/app/logic/common                         # shared logic mechanism (interfaces etc.)
    ${REPO_ROOT}/src/app/logic/common/communication/protocol  # wire formats: peripherals/, devices/, framing/, command/, system/, telemetry/
    ${REPO_ROOT}/src/app/platform                             # shared board-agnostic drivers
)

# Drop CubeMX's generated main.c — we own the entry point.
get_target_property(MX_SOURCES ${CMAKE_PROJECT_NAME} SOURCES)
list(FILTER MX_SOURCES EXCLUDE REGEX "/Core/Src/main\\.c$")
set_property(TARGET ${CMAKE_PROJECT_NAME} PROPERTY SOURCES "${MX_SOURCES}")

# Add sources to executable
target_sources(${CMAKE_PROJECT_NAME} PRIVATE
    # ECU board firmware: composition (main) + board-support, controller is header-only.
    ../main.cpp
    ../board.cpp

    # Shared logic mechanism the controller uses (src/app/logic/common).
    ${REPO_ROOT}/src/app/logic/common/control/persistent_state.cpp

    # Shared board-agnostic drivers the ECU wires (src/app/platform). No Ethernet.
    ${REPO_ROOT}/src/app/platform/memory/backup_ram.cpp
    ${REPO_ROOT}/src/app/platform/actuation/servomotor/hbl388.cpp
    ${REPO_ROOT}/src/app/platform/actuation/valve/ball_valve.cpp
    ${REPO_ROOT}/src/app/platform/acquisition/adc/ads131m08.cpp
    ${REPO_ROOT}/src/app/platform/acquisition/power_monitor/ina3221.cpp
    ${REPO_ROOT}/src/app/platform/communication/spi/spi_dil.cpp
    ${REPO_ROOT}/src/app/platform/communication/spi/spi_dma.cpp
    ${REPO_ROOT}/src/app/platform/communication/spi/spi_irq.cpp
    ${REPO_ROOT}/src/app/platform/storage/sd_card.cpp
    ${REPO_ROOT}/src/app/platform/communication/can/can_dil.cpp
    ${REPO_ROOT}/src/app/platform/data_integrity/crc/crc.cpp   # backs logic::data_integrity::crc32 (SdRecorder record CRC)
)

# Link directories setup
target_link_directories(${CMAKE_PROJECT_NAME} PRIVATE
    # Add user defined library search paths
)

# Add linked libraries
target_link_libraries(${CMAKE_PROJECT_NAME} PRIVATE
    # Add user defined libraries
)

# Remove wrong libob.a library dependency when using cpp files
list(REMOVE_ITEM CMAKE_C_IMPLICIT_LINK_LIBRARIES ob)

# Validate that STM32CubeMX code is compatible with C standard
if((CMAKE_C_STANDARD EQUAL 90) OR (CMAKE_C_STANDARD EQUAL 99))
    message(ERROR "Generated code requires C11 or higher")
endif()









