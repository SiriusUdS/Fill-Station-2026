build
mx.scratch
!.settings

# CM4 core is unused (CM7-only build). CubeMX may regenerate CM4/ from the dual-core
# .ioc; keep it untracked and unbuilt. (Phase 3: also boards/*/CM4/.)
CM4/

# Portable host toolchain for running logic unit tests (see tests/README.md)
tools/

# Host test build output
tests/build/
